#include <iostream>
#include <string>
#include <vector>
#include <queue>
using namespace std;

int main(){
    cin.tie(0);
    ios_base::sync_with_stdio(false);
    int T;cin>>T;
    for(int TT=0;TT<T;TT++){
        int n; cin>>n;
        int x[500], y[500];
        int p[500], q[500];
        for(int i=0;i<n;i++){
            cin>>x[i]>>y[i]>>p[i]>>q[i];
        }

    }
}